name: CI HTML

on:
  push:
    branches:
      - VERIF

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Test HTML
        run: |
          echo "Checking HTML"
          cat index.html
