name: Deploy

on:
  push:
    branches:
      - PRO

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Deploy
        run: echo "Deploying site..."
